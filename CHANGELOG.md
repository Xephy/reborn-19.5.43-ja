# 変更履歴

有志による非公式のファンメイド翻訳パッチです。詳細は [README](README.md#権利免責)。

バージョンは `v<対象のRebornバージョン>-ja.<パッチの版>` の形。
`v19.5.43-ja.11` は「Reborn 19.5.43 向けの11版目」を意味する。
Reborn 本体が更新されたら、対象バージョンの部分を上げて `-ja.1` から振り直す。

---

## v19.5.43-ja.11

翻訳が **82,524 / 82,619 行（99.9%）** になりました。差し替えるスクリプトは
**44本** のまま、`patch/` 以下も10個で変わりません。

### 修正: パスワードが日本語になっていて入力できなかった

パスワードは**プレイヤーが英字で打ち込むもの**なので、訳してはいけない文字列です。
訳してしまっていた箇所が2種類ありました。

**1. NPCが教えてくれるパスワード（16行）**

`Next time someone asks you for a password, tell them "nuzlocke"!` のような
セリフで、引用符の中身まで日本語にしていました。文章としては読めますが、
そのまま入力しても通りません。

| マップ | パスワード | 修正前 |
|---|---|---|
| メテオル遺跡 ほか | `unrealtime` | 無時間 |
| | `minlevel` | 最低レベル |
| | `antidote`（2行） | どくけし |
| | `stablweather` | せいじょうきこう |
| | `itemfinder` | アイテムサーチ |
| | `randomizer` | ランダマイザー |
| | `stopgains` | もう鍛えない |
| | `mischievous` | いたずら |
| | `monowoke` | モノウォーク |
| | `nuzlocke` | ヌズロック |
| | `penniless` | 文無し |
| | `yoink` | よいんく |
| | `rocketlocke` | ロケットロック |
| | `inversemode`（2行） | インバースモード |

`stablweather` は原文どおりのつづりです（`stable` ではありません）。直すと
パスワードとして通らなくなるため、そのままにしてあります。

`randomizer` は現在は受け付けられないパスワードですが、英字で入力すると
「もう入力できません」という専用の返事が返ります。カタカナのままだと
「パスワードではありません」になり応答が変わるので、これも英字に戻しました。

**2. パスワード一覧で `normal` が「通常」と表示されていた**

データチップで開くパスワード一覧で、モノタイプ挑戦の `normal` だけが
**未適用のときに**「通常」と出ていました。

一覧の行は「マーク＋パスワード」で組み立てられ、選択肢は1行ずつ翻訳表を
引き直します。未適用のマークは半角スペースなので、引くキーが裸の `normal` に
なって訳に当たっていました。適用中のマーク `> ` は空白ではないので当たりません。

その訳が要るのは `Debug.rb` の色違いラベル1か所だけで、そこはパッチの配布対象に
入っていません。`shiny` ともども訳を外しました。

### 翻訳: パスワード一覧の選択肢2行

同じ画面の `[Exit]` `[Add password]` が英語のままだったので
**「[やめる]」「[パスワードを追加]」** にしました。配列に積んでから
まとめて翻訳表を通す書き方で、呼び出し箇所を見る検査では拾えていませんでした。

### 検査: `check_passwords.py` を追加

パスワード212語について、次の2つを機械で見るようにしました。

- 一覧の行（未適用・適用中の両方の形）と同じキーの訳が登録されていないか
- 英文が引用符で囲んでいるパスワードが、訳文にそのまま残っているか

フィールドノートの見出し（`Cave` `Mountain` など）とアクセシビリティモード名
`Blindstep` は綴りが同じなだけなので、理由付きで除外しています。

## v19.5.43-ja.10

翻訳が **82,524 / 82,617 行（99.9%）** になりました。差し替えるスクリプトは
**44本** のまま、`patch/` 以下も10個で変わりません。

### 修正: マップイベントが自前で持つスクリプトの中の文字列が英語のままだった

スピネル博物館2階の化石復元の研究員に話しかけると、渡す化石を選ぶ
プロンプトだけが `Do you have a fossil for me to revive?` と英語で出ていました。

原因は**翻訳キーがどこにも登録されていなかった**ことです。イベントの中身は
こう書かれています。

```ruby
pbChooseItemFromList(
_I("Do you have a fossil for me to
revive?"),16, :HELIXFOSSIL, ...)
```

`_INTL` は通っているのに、`messages.dat` にキーがありません。Reborn の
コンパイラがキーを登録するのは**スクリプトファイルの中のリテラル**と
**マップの「文章の表示」コマンド**の2つで、マップイベントが自前で持つ
Ruby（コマンドコード355、続きが655）はどちらにも当たらないためです。

全マップを走査したところ、同じ原因のものが17か所・8種類ありました。

| 場所 | 内容 |
|---|---|
| スピネル博物館2階 | 化石復元のプロンプト |
| 賢者 | めざめるパワーの鑑定結果 |
| 育て屋 | タマゴが見つかったときの通知 |
| なまえ判定人の隣 | 生成した名前の感想 |
| アメ | 合言葉の入力 |
| キャス | 保存する技構成の名前入力 |
| フロリニアの診断（7か所） | 診断名の入力 |
| 開発室のオータム | 好きなポケモンの入力 |

引く先は呼び出し方で変わります。`_INTL` はスクリプトテキストの表を見ますが、
`_I` は**そのマップの表だけ**を見てスクリプトテキストにはフォールバックしません。
化石の1件だけマップ242の表に、残りはスクリプトテキストに入れてあります。

### 検査: マップイベントのスクリプトも走査するようにした

`find_untranslated.py` はこれまで `Scripts/` の中しか見ておらず、マップイベントが
持つ Ruby は盲点でした。`Data/Map*.rxdata` を走査して、リテラルごとに
**その呼び出しが実際に引く表**と突き合わせて報告するようにしています。
本体が更新されて新しいイベントが増えても、同じ型の漏れはこれで拾えます。

## v19.5.43-ja.9

翻訳が **82,516 / 82,609 行（99.9%）** になりました。差し替えるスクリプトが43本から
**44本** に増え、`patch/` 以下は10個で変わりません。

### 修正: どうぐを拾ったときの「ポケットにしまった」が英語のままだった

どうぐを入手すると「○○は ××を みつけた！」までは日本語で、直後の
`Xephy put the Rare Candy in the Medicine Pocket.` だけが英語で出ていました。
大広間でもらえるアメに限らず、**ゲーム中すべてのどうぐ入手**で起きていました。

原因は翻訳キーに Ruby の文字列展開が混ざっていたことです。

```ruby
_INTL("{1} put the ...<icon=bagPocket#{pocket}>...")
```

`#{pocket}` は `_INTL` に渡る**前**に展開されるため、登録されているキー
（`bagPocket#{pocket}` を含む文字列）と実行時に引かれるキー（`bagPocket2` など）が
永久に一致せず、英語がそのまま返っていました。

同じ原因で英語のまま残っていたバトル中のメッセージも回収しています。

- レッドカード / だっしゅつボタン / だっしゅつパックの発動
- からぶりほけん / のどスプレー / ルームサービス
- アロマベールがちょうはつ・いちゃもん・かなしばり・アンコールなどを防いだとき
- いのちがけが失敗したときの2行
- 努力値の入れ替え・上昇の確認、ボックス並べ替えの確認

なお、アロマベールが `Heal Block` を防いだ行の訳が「封じ込め」になっていたのを
**「かいふくふうじ」** に直しました。

### 独自の改善: 地方マップに地名の一覧を追加

ポケギアの地方マップで、地図を左に寄せて右に**全54地名の一覧**を出すようにしました。

- 一覧を上下に動かすと**地図のカーソルが追従**し、地図を動かすと**一覧が追従**します
- まだ行っていない場所はグレーで表示します
- そらをとぶで飛べる場所には印が付きます。そらをとぶの画面でも同じ一覧から選べます

操作は次のとおりです。画面右上に常時表示されます。

| 場面 | キー |
|---|---|
| 一覧（開いた直後） | `↑↓` 選択 / `←` 地図へ / `C` とぶ（そらをとぶの画面） |
| 地図 | 十字キーで移動 / `B` 一覧へ戻る |

新しく差し替えるスクリプトは `RegionMap.rb` の1本です。

## v19.5.43-ja.8

英語のまま残っていた箇所の回収です。翻訳は **82,475 / 82,568 行（99.9%）** のまま。
差し替えるスクリプトが40本から **43本** に増え、`patch/` 以下は10個で変わりません。

### 修正: 技まわりの画面でタイプのバッジが英語のままだった

技を忘れさせる画面・わざおもいだし・技教え・技構成の復元で、タイプのバッジが
英語表記のままでした。

タイプ名は画像に焼き込まれているため `Graphics/Icons/ja/` の日本語版に差し替える
仕組みになっていますが、この4画面だけは `AnimatedBitmap` にパスを直接渡していて、
差し替えを通っていませんでした。`pbBitmapName` を挟んで解決しています。

技を忘れさせる画面では、技のタイプは日本語・ポケモン自身のタイプだけ英語という
ちぐはぐな状態になっていました。わざおもいだしと技教えは全てのバッジが英語でした。

新しく差し替えるのは `MoveRelearner.rb` / `MoveTutor.rb` / `MovesetRestorer.rb` の3本です。

### 修正: 「メテオ団」の表記ゆれ

一部の会話で「ミート団」になっていた9か所を「メテオ団」に統一しました
（オブシディアスラム地下、北オブシディアワードなど）。

なお、原文が `TEAM MEATIER` と意図的にもじられている落書き2か所は、
言葉遊びとして「ミート団」のまま残しています。

## v19.5.43-ja.7

翻訳の変更はありません。操作性の追加が2件です。翻訳は **82,475 / 82,568 行（99.9%）**
のまま。差し替えるスクリプトは40本で変わらず、`patch/` 以下が9つから10個に増えています
（タイプ相性のアイコン画像を追加）。

### 独自の改善: 技を忘れさせる画面から「つよさ」を確認できるように

技を覚えさせるとき、どれを忘れさせるかはそのポケモンが物理寄りか特殊寄りかで
決まりますが、その画面ではこうげきもとくこうも見えませんでした。

**← / → でつよさページに切り替えられるようにしました。** 通常のポケモン詳細画面と
同じ `drawPageThree` をそのまま呼んでいるので、性格による能力の色分けまで同じに出ます。

- つよさページからは決定できません。B でも C でも技の一覧に戻るだけなので、
  ステータスを見ている最中に誤って技を消すことはありません
- 技忘れ画面はスプライトを最小限しか持っていなかったため、つよさページ用に
  立ち絵を追加しました。技選択中は非表示です

**これは翻訳ではなく挙動の追加です。** ゲームバランスには影響しませんが、
原作にはない動作なので README の「独自の改善」に明記しています。

### 独自の改善: タイプ相性のアイコン（既定は無効）

バトルの技選択ボタンと、交代画面から開いた「つよさ」のわざページに、相手への
通り方を記号で出せるようにしました。★4倍 ◎2倍 ○等倍 △1/2倍 ▼1/4倍 ×効果なし の6段階です。

**既定では無効で、パスワード `aishou` を入力すると有効になります。** Reborn 自身が
「任意の緩和はパスワードで」という設計を持っているので、それに乗せています。
知らない人にとっては存在しない機能として振る舞うため、原作の難易度は変わりません。

- スイッチ番号は使っていません。Reborn のスイッチは 2338 まで使用済みで 2335・2336 は
  開発者が予約しており、空きを取ると本体更新で衝突します。`$Settings` に保存しているため、
  セーブデータをまたいで有効になります
- 判定は技選択ボタンとつよさ画面で**同じ経路**を通します。別々に計算すると、同じ技なのに
  画面によって違う倍率が出ます
- とくせいは考慮しません。本家も見たことのないとくせいは教えないため、相性表だけを見る
  基準に揃えています。フィールドによるタイプ変化と相性表の書き換えは反映されます
- 記号は同梱フォントではなく図形として描いています。PixelMplus は10〜12ピクセル用の
  ドットフォントで、バッジの大きさまで拡大すると ○ や ◎ が角張った塊に崩れるためです

## v19.5.43-ja.6

同梱フォントで描画できない文字の修正。翻訳 **82,475 / 82,568 行（99.9%）**、
行数は前版と同じです。差し替えるファイルの構成は ja.5 と同じで、スクリプトの変更はありません。

見た目が正しくコードだけが違う文字が混ざっており、ゲーム内では豆腐（□）になっていました。
計355か所を修正しています。

- **波ダッシュ 340か所**。`U+301C 〜` を使っていたが、フォントに入っているのは
  `U+FF5E ～` のほう。伸ばし気味の語尾がすべて豆腐になっていた

- **「み」14か所**。とくせいの説明文で `U+202F`（狭いノーブレークスペース）に
  化けていた。「みかたのポケモンは〜」「きのみを〜」「かげをふみ〜」など

- **`丰` 1か所**。グリッチ演出でキの崩し字に使っていたが、常用漢字外でフォントに
  収録されていなかった。`干` に変更し、`力` と同じ「常用漢字を崩し字として流用する」
  形に揃えた

### 検査ツールの追加

`jp_translation/tools/check_font.py` を追加しました。フォントの cmap と
`japanese.dat` の全文字を突き合わせ、描画できない文字を報告します。
異常があれば終了コード1を返すので、ビルドのゲートに使えます。

この種の不具合は、エディタ上では正しく読めてゲーム内でだけ崩れるため、
目視の校正では発見できません。`check_joyo.py` と並べて走らせてください。

## v19.5.43-ja.5

バトルメッセージの取りこぼし修正。翻訳 **82,475 / 82,568 行（99.9%）**、
前版から +20 行。差し替えるファイルの構成は ja.4 と同じです。

- 壁や補助効果が切れるときのバトルメッセージ8種が英語だった（`The opposing team's
  Light Screen faded!` など）。`#{}` で組み立てていたためコンパイル時に登録される
  キーと実行時のキーが一致しなかった。`{1}` 形式に書き換えて解決

- 大ホールの手合わせ相手の敗北セリフを、マップ節に加えてセクション22にも登録した。
  `pbTrainerSpeech` はセクション16で外すと `_INTL` で引き直すので、`_I` の経路が
  何らかの理由で通らなくても訳が出る

## v19.5.43-ja.4

表を通していなかった箇所の回収。翻訳 **82,455 / 82,548 行（99.9%）**、
前版から +229 行。差し替えるスクリプトが39本から40本に増えています（`Trainers.rb` を追加）。

### 大ホールの手合わせ相手

- 敗北時のセリフ10人分が英語だった。マップイベントがゲーム変数へ素の文字列で
  代入しており、消費側は `_I` で引いているのに登録側が無く、訳を書く場所が
  存在しなかった。マップ38の節にキーを追加

### トレーナーの手持ちのニックネーム

- むしよけスプレーが切れた時の「もう一度使いますか?」の選択肢が英語（`Repel` /
  `Super Repel` / `Max Repel`）だった。どうぐ名をデータオブジェクトから生で
  取っており、表を通していなかった
- 最初のポケモンを選んだ時の確認メッセージで、種族名だけ英語だった
- PCの検索で、持ち物だけ英語名と照合していて日本語の検索語が当たらなかった
- ふつうのアメなど、まとめて使えるどうぐの「いくつ使いますか?」が英語だった。
  表示文字列が補助関数の第2引数にあり、しかも単一引用符だったため登録されていなかった
- `find_untranslated.py` が `$cache.<表>[...].name` の生参照を検出するようにした
- `find_untranslated.py` が補助関数の第2引数以降のリテラルも見るようにした
- ジムリーダーなどの手持ちに付いたニックネームが英語のままだった（シェリーの
  メガヤンマ `Heather`、バルビート `Bugsy` など）。`trainers.dat` の生の英語が
  生成時の訳を上書きしていた。表を通すようにし、**229種類のうち218種類**を翻訳
  （37トレーナー・404体分）。字形そのものが内容の11件（`CL:4R1-C3` `:)` `No.1`〜`No.6`）は
  カタカナにすると成立しないため英語のまま

## v19.5.43-ja.3

ポケギアの「時間と天気」と操作設定の日本語化。翻訳 **82,226 / 82,319 行（99.9%）**、
前版から +78 行。差し替えるスクリプトが37本から39本に増えています。

### ポケギア「時間と天気」

- 日付が `strftime` の英語表記のままだった（`Wed  12  Aug  -  Day`）。曜日・月の
  部品と並び順のテンプレートをセクション22に追加し、`8月12日 (水)  -  昼` の順に
  組み替えるようにした
- 時間帯（`Day` / `Night` / `Evening` / `Morning`）は `_INTL` を通っていなかったので
  キーごと追加
- 背景画像に焼き込まれた `CURRENT WEATHER` / `WEATHER AT` を日本語に描き直した
  （「現在の天気」「次の天気」）。`IconSprite#setBitmap` は `pbResolveBitmap` を
  通らないので、`pbBitmapName` 経由に変更している

### 操作設定

- キーボード／ゲームパッドの操作一覧（48行）を日本語にした。`pbDrawTextPositions` で
  直接描かれる素の文字列で、メッセージ関数を通らないため従来の検査に掛からなかった
- セーブ選択画面の `Normal Save` / `Autosave`、オープニングの切符の `Grandview Station`
  も同じ検査で見つかったので対応した

### Reborn 側の不具合修正

- 天気アイコンが表示されない問題を修正。天気の名前が `_INTL` を通ったうえで
  `weather_%s` のファイル名に埋め込まれており、訳が入っているとパスが存在しない
  ファイルを指していた。`GifBitmap` が読み込み失敗を握りつぶすためエラーも出ずに
  消えていた

## v19.5.43-ja.2

前版で英語のまま残っていた箇所の修正。翻訳 **82,148 / 82,241 行（99.9%）**、
前版から +1,550 行。

**新たに翻訳**

- バトルタワー系トレーナーのセリフ 1,053件と名前 309件（`bttrainers.dat`。
  どのメッセージセクションにも登録されておらず、キーごと存在しなかった）
- ポケモン図鑑の分類・図鑑説明・一覧の名前（訳は 589件・839件そろっていたのに
  データオブジェクトから直接読んでいた）
- エリア移動時の地名、セーブ画面のマップ名、「出会った場所」（マップ名の
  メッセージセクションを読むコードが1つも無かった）
- トレーナーの戦闘後のセリフ（771件の訳を画面に出す側だけが読んでいなかった）
- バトルの能力変化メッセージ。「リザードンの攻撃がぐーんと上がった!」
  （能力名と「ぐーんと」「がくっと」がベタ書きの英語だった）
- バトルの Inspect 画面（能力値ラベル、天気・じゅうりょくの残りターン）
- フィールドノート一覧のフィールド名と分類見出し（`RebornPokegear.rb` が
  `Scene_FieldNotes` を開き直してメニューを差し替えており、そちらが未対応だった）
- フィールドで毒に耐えきったときのメッセージと、隠しアイテムの入手メッセージ
- パスワード入力まわり、休憩・時間待ち、にじいろのかおりのメッセージ 27件
  （`_INTL('…')` とシングルクォートで書かれており、Reborn のスクレイパが
  ダブルクォートしか見ないためキーが登録されていなかった）

**画像の作り直し**（文字が絵に描き込まれているもの）

- バトルの行動選択ボタン（たたかう／バッグ／ポケモン／にげる）
- 図鑑のタイプバッジ 19枚
- 状態異常バッジ（ねむり／どく／やけど／まひ／こおり／ひんし）

**Reborn 側の不具合修正**

- 図鑑の頭文字検索を ア行〜ラワ行に置き換え、五十音順の並べ替えに対応
- コンパイル時と実行時でキーが食い違う3件（`\\n` を含むリテラル）
- `(_INTL "…")` という書き方がスクレイパに拾われずキーが登録されない問題

**既知の未対応**

- 図鑑と検索画面の枠の `EXIT` `NAV` `SEARCH`（背景が単色でなく差し替え不可）
- ポケルスのバッジは `PKRS` のまま（44pxの枠に4文字が収まらない）

---

## v19.5.43-ja.1

初回リリース。Pokémon Reborn 19.5.43 の全編日本語化。

**翻訳** — 80,598 / 80,691 行（99.9%）

- ストーリー会話・イベント・NPCのセリフ（全マップ、67,255行）
- ポケモン・わざ・とくせい・どうぐ・分類・タイプの名前と説明文（公式日本語名）
- バトルメッセージ、トレーナーの戦闘前後のセリフ
- フィールドノート 882件、バトル中のフィールド効果メッセージ 229件
- パルス図鑑、タウンマップ、オプション、PCボックス、セーブ画面

**表示まわりの対応**

- かな・漢字を持つフォント（M+ FONTS）の同梱と、禁則処理つきの自動改行
- 幅に収まらない訳文の自動縮小（わざ選択、とくせいの説明、パルス図鑑など）
- タイプアイコン19枚とパルス図鑑14枚の作り直し（文字が絵に描き込まれているため）

**Reborn 側の不具合修正**

- フィールドノートのコンパイルが言語に依存していた問題。日本語で初回起動すると
  `<icon=typeでんき>` という存在しないアイコン名が焼き付き、タイプアイコンが
  すべて消えていた
- マップ名のメッセージセクションを誰も読んでいなかった問題。エリア移動時の看板・
  セーブ画面・「出会った場所」が翻訳データを無視していた
- `_INTL` を通さず素の文字列を表示している約150箇所
